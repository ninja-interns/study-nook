import { Button } from "@material-ui/core";


export function StartNookButton() {
    return (
        <div>
            <Button variant="contained" color="primary" component="span">
                Start nooking
            </Button>
        </div>
    );
}

export function AchievementButton() {
    return (
        <div>
            <Button variant="contained" color="primary" component="span">
                Latest Badge
            </Button>
        </div>
    );
}