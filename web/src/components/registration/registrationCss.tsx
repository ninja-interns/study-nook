import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
    container: {
        marginLeft: "50px",
        marginRight: "50px",
        alignContent: 'center',
        textAlign: 'center',
    },
});
